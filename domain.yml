version: "2.0"

intents:
  - greet
  - bye
  - bot
  - fallback_intent
  - faq:
      is_retrieval_intent: true
      
session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
